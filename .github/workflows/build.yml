name: Build

on: push

jobs:
  build:
    name: Phar building
    runs-on: self-hosted
    if: "contains(github.event.head_commit.message, '[Build]')"

    steps:
      - name: Startup
        uses: actions/checkout@v2
      - name: Checkout PharBuilder
        uses: actions/checkout@v2
        with:
          repository: NetherGamesMC/PharBuilder
          token: ${{ secrets.ng_token }}
          path: PharBuilder
          ref: PMMP4.0
      - name: Download PHP Release
        uses: dsaltares/fetch-gh-release-asset@0.0.5
        with:
          file: PHP_Linux.tar.gz
          repo: NetherGamesMC/php-build-scripts
          version: "tags/8.0.3"
      - name: Untar PHP Release
        run: tar -xzvf PHP_Linux.tar.gz
      - name: Build
        run: |
          echo Building...
          chmod +x PharBuilder/BuildScript.php
          ./bin/php7/bin/php PharBuilder/BuildScript.php --make "" --out PharBuilder/NGPlots.phar
          echo Build completed!
      - name: Upload
        uses: actions/upload-artifact@v1
        with:
          name: NGPlots
          path: PharBuilder/NGPlots.phar
